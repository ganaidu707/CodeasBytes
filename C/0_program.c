#include<stdio.h>

// Interview questions for beginers
// bascic operations in c
int main() {
    int a = 10, b = 5;

    printf("add: %d\n", a + b);
    printf("sub: %d\n", a - b);
    printf("mul: %d\n", a * b);
    printf("div: %d\n", a / b);

    return 0;
}